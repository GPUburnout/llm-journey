baseURL = 'https://gpuburnout.com/'
languageCode = 'en-us'
title = 'GPUburnout | Jun Park'
theme = 'PaperMod'
[pagination]
  pagerSize = 10

[params]
  env = "production"
  description = "Building LLMs from scratch: a multi-season journey through transformer architecture, PyTorch implementation, and machine learning optimization. Season 1: GPT-2 (134M). Season 2: Llama (1B). Every lesson documented."
  author = "GPUburnout"
  keywords = ["machine learning", "LLM", "GPT-2", "Llama", "training", "transformer", "deep learning", "PyTorch", "NLP", "language model", "AI tutorial", "from scratch"]
  images = ["/images/og-default.png"]

  # Visual polish
  defaultTheme = "dark"
  disableThemeToggle = false

  # Layout options
  showReadingTime = true
  showShareButtons = true
  showPostNavLinks = true
  showBreadCrumbs = true
  showCodeCopyButtons = true
  showToc = true
  TocOpen = true

  # Homepage profile mode (more polished than info mode)
  [params.profileMode]
    enabled = true
    title = "Building LLMs From Scratch"
    subtitle = "A life scientist who got curious about transformers — the neural network kind, not the protein kind — and decided to build one from scratch."
    imageUrl = "https://raw.githubusercontent.com/googlefonts/noto-emoji/main/svg/emoji_u1f916.svg"
    imageWidth = 140
    imageHeight = 140
    imageTitle = "LLM Journey"

    [[params.profileMode.buttons]]
      name = "Read the Journey"
      url = "posts/"

    [[params.profileMode.buttons]]
      name = "View on GitHub"
      url = "https://github.com/GPUburnout"

  [[params.socialIcons]]
    name = "github"
    url = "https://github.com/GPUburnout"

  # Cover image defaults
  [params.cover]
    hiddenInList = false
    hiddenInSingle = false

[params.editPost]
  URL = "https://github.com/GPUburnout/llm-journey/tree/main/content"
  Text = "Suggest Changes"
  appendFilePath = true

[markup]
  [markup.highlight]
    codeFences = true
    guessSyntax = true
    lineNos = true
    style = "monokai"

  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

# Sitemap settings for SEO
[sitemap]
  changefreq = "weekly"
  priority = 0.5
  filename = "sitemap.xml"

[outputs]
  home = ["HTML", "RSS", "JSON"]

[[menu.main]]
  identifier = "posts"
  name = "Posts"
  url = "posts/"
  weight = 10

[[menu.main]]
  identifier = "tags"
  name = "Tags"
  url = "tags/"
  weight = 20

[[menu.main]]
  identifier = "archives"
  name = "Archives"
  url = "archives/"
  weight = 30

[[menu.main]]
  identifier = "about"
  name = "About"
  url = "about/"
  weight = 35

[[menu.main]]
  identifier = "search"
  name = "Search"
  url = "search/"
  weight = 40
